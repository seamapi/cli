import type { Blueprint } from '@seamapi/blueprint'

// Replaced with the generated blueprint when the package is packed.
const seamapiBlueprint: Blueprint | null = null

export default seamapiBlueprint
